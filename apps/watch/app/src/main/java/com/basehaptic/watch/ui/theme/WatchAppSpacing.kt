package com.basehaptic.watch.ui.theme

import androidx.compose.ui.unit.dp

/**
 * 워치 전용 정적 스페이싱 토큰.
 * 디바이스 크기별 반응형 값은 `WatchUiProfile`을 사용하고,
 * 프로파일과 무관한 정적 상수는 여기서 가져온다.
 * Mobile `AppSpacing`과 동일 스케일.
 */
object WatchAppSpacing {
    val xxs = 2.dp
    val xs = 4.dp
    val sm = 8.dp
    val md = 12.dp
    val lg = 16.dp
    val xl = 20.dp
    val xxl = 24.dp
    val xxxl = 32.dp
}
