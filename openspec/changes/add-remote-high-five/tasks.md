## 1. 설계 확정 및 사전 작업

- [ ] 1.1 `add-community-tab` 오너와 커뮤니티 피드 아이템 타입 합의 (신규 타입 vs 기존 카드 카운트 필드)
- [ ] 1.2 BLE 광고 페이로드 포맷 문서화 (Service UUID 1개 할당, Service Data 바이트 레이아웃, protocol_version=1)
- [ ] 1.3 파일럿 팀 플래그 키 정의 (서버 원격 플래그 + 빌드 상수 이중 게이트)
- [ ] 1.4 RSSI 임계값 초기값(-80 dBm)과 튜닝 플래그 정의
- [ ] 1.5 개인정보 처리방침 초안 문구 작성 ("근처 팬 감지 BLE, 위치 raw 미수집")

## 2. 공통 도메인 모델

- [ ] 2.1 팀 ID → 1바이트 식별자 매핑 테이블 정의 (KBO 10개 구단)
- [ ] 2.2 이벤트 종류 enum 정의 (1=득점, 2=홈런)
- [ ] 2.3 회전 토큰 생성·교체 규칙 정의 (60초 주기, 보안 난수)
- [ ] 2.4 하이파이브 성립 집계 dedupe 규칙(동일 토큰 1건) 문서화

## 3. iOS Mobile (CoreBluetooth)

- [ ] 3.1 `Info.plist`에 `NSBluetoothAlwaysUsageDescription` 문구 추가 (위치 미수집 명시)
- [ ] 3.2 CoreBluetooth central+peripheral 매니저 모듈 신설
- [ ] 3.3 이벤트 트리거 훅 연결: 내 팀 득점·홈런 수신 시 윈도우 open (≤20초)
- [ ] 3.4 60초 이상 지연 이벤트 무시 로직
- [ ] 3.5 광고 페이로드 빌더 (팀ID/이벤트종류/회전토큰/버전)
- [ ] 3.6 스캔 콜백에서 Service UUID 필터링 + RSSI 임계값 판정
- [ ] 3.7 하이파이브 성립 시 WCSession으로 워치 트리거 메시지 송신
- [ ] 3.8 SettingsScreen에 토글 추가 (파일럿 팀일 때만 노출)
- [ ] 3.9 토글 on 시 블루투스 권한 요청, 거부 시 토글 자동 복구
- [ ] 3.10 토글 off 시 진행 중 광고·스캔 즉시 중단

## 4. Android Mobile (BluetoothLeAdvertiser/Scanner)

- [ ] 4.1 `AndroidManifest.xml`에 `BLUETOOTH_SCAN`, `BLUETOOTH_ADVERTISE` 권한 추가 (`neverForLocation` 플래그)
- [ ] 4.2 Android 11 이하는 토글 숨김 처리 (SDK 체크)
- [ ] 4.3 BLE 광고/스캔 매니저 모듈 신설
- [ ] 4.4 이벤트 트리거 훅 연결: `realtime` 수신 지점에서 윈도우 open (≤20초)
- [ ] 4.5 60초 이상 지연 이벤트 무시 로직
- [ ] 4.6 광고 페이로드 빌더 (iOS와 바이트 레이아웃 동일)
- [ ] 4.7 스캔 결과에서 Service UUID 필터 + RSSI 임계값 판정
- [ ] 4.8 하이파이브 성립 시 Wear Data Layer로 워치 트리거 메시지 송신
- [ ] 4.9 SettingsScreen에 토글 추가 (파일럿 팀일 때만 노출, `SettingsItemWithSwitch` 재사용)
- [ ] 4.10 토글 on 시 권한 요청, 거부 시 토글 자동 복구
- [ ] 4.11 토글 off 시 진행 중 광고·스캔 즉시 중단

## 5. Watch — 전용 하이파이브 햅틱 패턴

- [ ] 5.1 iOS watchOS: 하이파이브 전용 햅틱 시퀀스 등록 (기존 득점 패턴과 분리)
- [ ] 5.2 Wear OS: `VibrationEffect` 기반 하이파이브 패턴 등록
- [ ] 5.3 폰에서 온 "하이파이브 트리거" 메시지 수신 핸들러 (양 플랫폼)
- [ ] 5.4 기존 득점 햅틱 재생 직후 하이파이브 패턴이 재생되는지 순서 검증

## 6. 커뮤니티 탭 배지 기록

- [ ] 6.1 커뮤니티 피드 아이템 타입 합의 결과 반영 (신규 타입 또는 카운트 필드)
- [ ] 6.2 성립 건수 > 0일 때 윈도우 종료 시점에 배지 1건 기록
- [ ] 6.3 배지 레코드에 상대 식별자·위치 미포함 검증 (스키마 레벨)
- [ ] 6.4 파일럿 팀(LG/두산) 한정 게이트 적용

## 7. 플래그 / 파일럿 게이트

- [ ] 7.1 서버 원격 플래그 키 추가 (`remote_high_five_pilot_teams`)
- [ ] 7.2 앱에서 플래그 pull → 설정 토글 노출/숨김 반영
- [ ] 7.3 플래그 off 전환 시 진행 중 윈도우 즉시 종료
- [ ] 7.4 빌드 상수 이중 게이트 검증

## 8. 테스트

- [ ] 8.1 단위 테스트: 페이로드 빌더·파서 (iOS/Android 상호 호환 바이트 케이스)
- [ ] 8.2 단위 테스트: 매칭 규칙 (팀/이벤트/RSSI 경계값, 토큰 중복)
- [ ] 8.3 단위 테스트: 지연 이벤트·평상시 광고 금지
- [ ] 8.4 통합 테스트: iOS ↔ Android 광고 수신 페어 테스트 (실제 디바이스 2대)
- [ ] 8.5 통합 테스트: 이벤트 시뮬레이터로 윈도우 open/close 시나리오
- [ ] 8.6 UI 테스트: 비파일럿 팀 응원 시 토글 숨김
- [ ] 8.7 UI 테스트: 토글 on/off, 권한 거부 플로우
- [ ] 8.8 수동 테스트: 기존 득점 햅틱 직후 하이파이브 햅틱 재생 체감 검증

## 9. 문서화 및 배포

- [ ] 9.1 개인정보 처리방침 업데이트 ("원격 하이파이브" 섹션, BLE 사용 목적, 위치 미수집)
- [ ] 9.2 스토어 설명 문구에 신규 기능 추가 (파일럿 표현)
- [ ] 9.3 사내 드로그푸드 실시 (이벤트 시뮬레이터 + 실기기 2대)
- [ ] 9.4 파일럿 릴리즈 (LG/두산 플래그 on)
- [ ] 9.5 오남용 로그 대시보드 항목 추가 (과도한 RSSI, 비정상 페이로드)
- [ ] 9.6 파일럿 지표(토글 전환율, 하이파이브 성립 수, 배지 조회수) 모니터링 후 전 구단 확대 결정
